## Prerequisites

NodeJS >= 24

## Install dependencies

```sh
pnpm install
```

## How to use

- Run `node src/0-init-directories.ts`
- Copy your photos in `pictures/library`
- Run `node src/1-scan.ts`
- Copy one or several reference photos in `pictures/reference`
- Run `node src/2-find.ts`
